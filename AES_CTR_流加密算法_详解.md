AES-CTR 流加密算法

数学原理：异或：相同为0不同为1；可逆性：A⊕B⊕B =A
工作原理： 
1、通过AES算法,使用密钥、初始向量IV和递增计数器, 生成16字节的密钥流
2、用 16字节的密钥流 异或 16个字节的明文 得到了16字节的密文
3、循环这么操作，就能以16字节为单位，对一段明文进行，逐16字节的加密
4、解密时，通过相同的密钥、初始向量IV和递增计数器, 生成与加密时相同16字节的密钥流
4、解密时，然后 16字节的密钥流 异或 16个字节的密文 得到了16字节的明文

库实现
AES 标准 (FIPS 197) ：AES 一次处理 16 字节
CTR 标准 (NIST SP 800-38A),流密码的定义：CTR 支持任意长度输入（0字节~只要计数器不溢出）
所以CTR标准天然定义了，不需要对齐16字节，框架一般通过缓存密钥流实现。具体为：不足16字节的加解密，假设3字节：计算出16字节的密钥流，取密钥流前3个字节，与明文/密文做异或运算，返回给调用方。剩余的密钥流留着，直到消耗完毕。下一次再用AES计算新的16字节的密钥流。
安全性保证：
根据 NIST Special Publication 800-38A  标准，https://nvlpubs.nist.gov/nistpubs/Legacy/SP/nistspecialpublication800-38a.pdf，Section 6.5 (CTR Mode 章节)
描述："The sequence of counters must have the property that each block in the sequence is different from every other block."计数器序列必须具有这样的性质：序列中的每一个块都不同于所有其他的块。（这里的计数器序列指的是 iv和count的组合）
AES-CTR的核心加密操作为：密文等于“明文 逐字节异或 AES算法对密钥、初始向量IV和递增计数器三者组合产生的密钥流”，由于异或运算的自反性，解密时只需用同一密钥流再次异或密文即可还原明文；
在单次处理大文件时，IV固定随机生成，计数器从0开始每处理16字节自动加1，因此送入AES的(IV,计数器)组合在本次任务中始终保持递增且绝不重复，从而保证了密钥流的唯一性；
当需要加密新的独立数据时，允许沿用同一把密钥，计数器也可以重新从0开始，但必须为新任务重新生成一个全新的随机IV，只要新旧任务的IV不同，那么两次加密中所有(IV,计数器)组合构成的集合就不会有任何交集，进而确保在同一密钥生命周期内送入AES的每个输入块都全局唯一，安全性由此得到严格保障。

公开性与保密性：
AES-CTR加解密需要的参数有且仅有3个（key,iv,count）;
key为密钥，是由用户自行生成的32字节的密钥后保存，需要保证高熵，可以使用os.urandom(32)生成；
iv为初始向量，标准要求，不同的加密任务，iv应当不同且随机，iv不需要保密，一般和密文一起保存；
count为计数器，AES流式加密，每16字节会加密一次，计数器+1，以保证每次加密(,iv,count)不重复；
- 在这三个参数中，key需要保密，iv不需要保密，count不需要保密，coungt一般由框架自动封装了相关逻辑，用户大部分需求无需感知count


疑难杂症：
超过一字节的整数，在二进制流中，是高位在前还是低位在前，没有统一强制规范。虽然NIST 标准约定了counter是大端实现。但是还是有些库提供了小端实现。
在CTR计算密钥时是，使用的counter是整数类型，如果加密和解密的库，一个是大端实现，一个是小端实现，会出现计算出密钥流不同的情况，无法解密。所以，加解密最好使用同一个库。

1. 关于 IV 复用的真实定义
结论：
CTR 模式的“IV 不可复用”并非禁止外层 IV 保持不变，而是禁止送入 AES 算法的完整输入块 (IV, Counter) 出现重复。单个文件内 IV 固定但 Counter 递增，送入 AES 的每一组值都是全新的，因此绝对安全。
依据：
密钥流块 = AES(Key, IV || Counter)。只要 (Key, IV, Counter) 三元组不同，生成的密钥流就不同，机密性不受任何影响。
--------------------------------------------------------------------------------
2. 关于密文长度等于明文长度的实现
结论：
CTR 不对明文进行填充，而是生成固定 16 字节的密钥流后按需截取。末尾不足 16 字节时只取所需长度参与异或，多余密钥流直接丢弃，因此密文长度始终严格等于明文长度。
依据：
设明文末段长度为 L（1 ≤ L ≤ 16），则
密文末段 = 明文末段 ⊕ AES(Key, IV+Count)[0 : L]，输出长度即为 L。
--------------------------------------------------------------------------------
3. 关于外层循环后 Counter 的推进步长
结论：
Counter 的递增单位是“AES 分组数（16 字节）”，而非“函数调用次数”。外层每轮读取 N 字节，内部 AES 执行了 N/16 次，消费了同等数量的 Counter 值，因此外层循环结束后必须推进 N/16 步。
依据：
每处理 16 字节消耗 1 个 Counter。若本轮读入 32 字节，内部依次使用 Count 和 Count+1，循环结束后应执行 Count = Count + 32/16（即 Count + 2）。若只加 1，将导致下一轮重用上一轮的密钥流，引发局部安全性漏洞。
--------------------------------------------------------------------------------
4. 关于“全局唯一”中“全局”的度量范围
结论：
“全局唯一”限定在“同一把密钥（Key）的生命周期内”，并不要求跨文件强制连续递增。只要两个文件的 IV 不同，即使 Counter 都从 0 开始，两组 (IV, Counter) 集合也不会有交集，完全符合安全标准。
依据：
唯一性判定标准是 (IV, Counter) 二元组。当 IV1 ≠ IV2 时，(IV1, 0) ≠ (IV2, 0)，因此两个文件的密钥流集合互不重叠，安全成立。
--------------------------------------------------------------------------------
5. 关于跳块解密的可行性（随机访问）
结论：
CTR 天然支持不解密前序块、直接跳转到任意位置解密，因为每个块的解密运算完全独立，不存在 CBC 模式那种链式依赖。
依据：
解密公式为 Plain_i = Cipher_i ⊕ AES(Key, IV + i)。该式右侧仅依赖当前块的密文和当前索引 i，与 i-1、i-2 等前序块无关，因此可从任意 Count 值起始直接解密目标块。
